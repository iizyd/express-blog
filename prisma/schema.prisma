generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model blog_article {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  title           String?   @default("") @db.VarChar(100)
  desc            String?   @default("") @db.VarChar(255)
  cover_image_url String?   @default("") @db.VarChar(255)
  content         String?   @db.LongText
  created_on      DateTime? @default(now()) @db.DateTime(0)
  created_by      String?   @default("") @db.VarChar(100)
  modified_on     DateTime? @default(now()) @db.DateTime(0)
  modified_by     String?   @default("") @db.VarChar(100)
  deleted_on      DateTime? @db.DateTime(0)
  is_del          Int?      @default(0) @db.UnsignedTinyInt
  state           Int?      @default(1) @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model blog_article_tag {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  article_id  Int
  tag_id      Int       @default(0) @db.UnsignedInt
  created_on  DateTime? @default(now()) @db.DateTime(0)
  created_by  String?   @default("") @db.VarChar(100)
  modified_on DateTime? @default(now()) @db.DateTime(0)
  modified_by String?   @default("") @db.VarChar(100)
  deleted_on  DateTime? @db.DateTime(0)
  is_del      Int?      @default(0) @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model blog_tag {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  name        String?   @default("") @db.VarChar(100)
  created_on  DateTime? @default(now()) @db.DateTime(0)
  created_by  String?   @default("") @db.VarChar(100)
  modified_on DateTime? @default(now()) @db.DateTime(0)
  modified_by String?   @default("") @db.VarChar(100)
  deleted_on  DateTime? @db.DateTime(0)
  is_del      Int?      @default(0) @db.UnsignedTinyInt
  state       Int?      @default(1) @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  user_name   String?   @default("") @db.VarChar(20)
  password    String?   @default("") @db.VarChar(50)
  created_on  DateTime? @default(now()) @db.DateTime(0)
  created_by  String?   @default("") @db.VarChar(100)
  modified_on DateTime? @default(now()) @db.DateTime(0)
  modified_by String?   @default("") @db.VarChar(100)
  deleted_on  DateTime? @db.DateTime(0)
  is_del      Int?      @default(0) @db.UnsignedTinyInt
}
